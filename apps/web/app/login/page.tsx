'use client';
import { useState, type FormEvent } from 'react'; import { useAuth } from '../../lib/auth-context'; import { ApiError } from '../../lib/api';
export default function LoginPage() { const { login, loading } = useAuth(); const [identifier, setIdentifier] = useState(''); const [password, setPassword] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const submit = async (e: FormEvent) => { e.preventDefault(); if (busy) return; setBusy(true); setError(''); try { await login(identifier.trim(), password); } catch (err) { setError(err instanceof ApiError ? err.message : 'Unable to sign in. Check that the backend is running.'); } finally { setBusy(false); } }; return <main className="auth-page"><section className="auth-visual"><div className="auth-brand"><span className="brand-mark">D</span><strong>DAVI</strong></div><div><span className="eyebrow">SCHOOL OPERATIONS, SIMPLIFIED</span><h1>Build a stronger school, one decision at a time.</h1><p>Manage academics, people, and access from a secure workspace designed for school leaders.</p></div><small>© 2026 DAVI · The AI Operating System for Schools</small></section><section className="auth-panel"><form className="auth-card" onSubmit={submit}><div className="mobile-brand"><span className="brand-mark">D</span><strong>DAVI</strong></div><span className="eyebrow">WELCOME BACK</span><h2>Sign in to your school</h2><p>Use your email address, mobile number, or username.</p>{error && <div className="alert error">{error}</div>}<label className="field"><span>Email, mobile, or username</span><input autoFocus autoComplete="username" value={identifier} onChange={e => setIdentifier(e.target.value)} placeholder="admin@school.edu" required /></label><label className="field"><span>Password</span><input type="password" autoComplete="current-password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" required /></label><button className="button full" disabled={busy || loading}>{busy ? 'Signing in…' : 'Sign in'}</button><p className="secure-note">Secure access protected by DAVI authentication</p></form></section></main>; }
