'use client';
import{useEffect,useState,type FormEvent}from'react';import{ApiError,staffApi,teacherApi}from'../../../../lib/api';import{useAuth}from'../../../../lib/auth-context';import{useToast}from'../../../../lib/toast-context';import{EmptyState,Field,FormActions,LoadingState,Modal,PageHeader,StatusBadge}from'../../../../components/ui';
export default function TeachersPage(){const{session}=useAuth();const{show}=useToast();const[rows,setRows]=useState<Awaited<ReturnType<typeof teacherApi.list>>>([]);const[staff,setStaff]=useState<Awaited<ReturnType<typeof staffApi.list>>>([]);const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[form,setForm]=useState({userId:'',teacherCode:'',specialization:'',qualification:''});const load=async()=>{if(!session?.user.schoolId)return;try{const[a,b]=await Promise.all([teacherApi.list(session.token),staffApi.list(session.token,session.user.schoolId)]);setRows(a);setStaff(b)}catch(e){show(e instanceof ApiError?e.message:'Teachers could not be loaded.','error')}};useEffect(()=>{void load()},[session?.token]);const save=async(e:FormEvent)=>{e.preventDefault();if(!session?.user.schoolId)return;setBusy(true);try{await teacherApi.create(session.token,{...form,schoolId:session.user.schoolId});show('Teacher profile created.');setOpen(false);await load()}catch(e){show(e instanceof ApiError?e.message:'Teacher could not be created.','error')}finally{setBusy(false)}};return <><PageHeader title="Teachers" description="Create teaching profiles from existing staff accounts." action={<button className="button" onClick={()=>setOpen(true)}>+ Add teacher</button>}/>{!rows.length?<EmptyState title="No teachers created" detail="Add a staff member, then create their teacher profile."/>:<section className="card table-wrap"><table><thead><tr><th>Teacher</th><th>Code</th><th>Specialization</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.user.firstName} {r.user.lastName}</td><td>{r.teacherCode}</td><td>{r.specialization||'—'}</td><td><StatusBadge status={r.status}/></td></tr>)}</tbody></table></section>}{open&&<Modal title="Create teacher profile" onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={save}><Field label="Staff member" required><select required value={form.userId} onChange={e=>setForm({...form,userId:e.target.value})}><option value="">Select staff</option>{staff.filter(s=>!rows.some(t=>t.userId===s.userId)).map(s=><option value={s.userId} key={s.id}>{s.user.firstName} {s.user.lastName} · {s.employeeId}</option>)}</select></Field><Field label="Teacher code" required><input required value={form.teacherCode} onChange={e=>setForm({...form,teacherCode:e.target.value})}/></Field><Field label="Specialization"><input value={form.specialization} onChange={e=>setForm({...form,specialization:e.target.value})}/></Field><Field label="Qualification"><input value={form.qualification} onChange={e=>setForm({...form,qualification:e.target.value})}/></Field><FormActions submitting={busy} onCancel={()=>setOpen(false)} label="Create teacher"/></form></Modal>}</>}
