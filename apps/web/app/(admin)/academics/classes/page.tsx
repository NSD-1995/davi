'use client';
import { useEffect, useMemo, useState, type FormEvent } from 'react'; import { ApiError, classApi, sectionApi } from '../../../../lib/api'; import { useAuth } from '../../../../lib/auth-context'; import { useAcademicYear } from '../../../../lib/academic-year-context'; import { useToast } from '../../../../lib/toast-context'; import { EmptyState, Field, FormActions, LoadingState, Modal, PageHeader, PermissionGate, StatusBadge } from '../../../../components/ui'; import type { SchoolClass, Section } from '../../../../lib/types';
export default function ClassesPage() { const { session } = useAuth(); const { selected } = useAcademicYear(); const { show } = useToast(); const [rows,setRows]=useState<SchoolClass[]>([]); const [loading,setLoading]=useState(true); const [modal,setModal]=useState<'class'|'section'|'bulk'|null>(null); const [target,setTarget]=useState<SchoolClass|Section|null>(null); const [busy,setBusy]=useState(false); const [classForm,setClassForm]=useState({name:'',code:'',capacity:'',status:'ACTIVE'}); const [sectionForm,setSectionForm]=useState({name:'',capacity:'',status:'ACTIVE'}); const [bulk,setBulk]=useState({montessori:false,primary:false,secondary:false,seniorSecondary:false,numberOfSections:1,sectionCapacity:''}); const load=async()=>{if(!session)return;setLoading(true);try{setRows(await classApi.list(session.token));}catch(e){show(e instanceof ApiError?e.message:'Classes could not be loaded.','error');}finally{setLoading(false);}}; useEffect(()=>{void load();},[session?.token]); const visible=useMemo(()=>rows.filter(r=>r.academicYearId===selected?.id),[rows,selected]); const openClass=(row?:SchoolClass)=>{setTarget(row||null);setClassForm(row?{name:row.name,code:row.code||'',capacity:String(row.capacity||''),status:row.status}:{name:'',code:'',capacity:'',status:'ACTIVE'});setModal('class');}; const openSection=(parent:SchoolClass,row?:Section)=>{setTarget(row||parent);setSectionForm(row?{name:row.name,capacity:String(row.capacity||''),status:row.status}:{name:'',capacity:'',status:'ACTIVE'});setModal('section');}; const saveClass=async(e:FormEvent)=>{e.preventDefault();if(!session||!selected)return;setBusy(true);const data={academicYearId:selected.id,name:classForm.name,code:classForm.code||undefined,capacity:classForm.capacity?Number(classForm.capacity):undefined,status:classForm.status};try{if(target&&'academicYearId'in target)await classApi.update(session.token,target.id,data);else await classApi.create(session.token,data);show('Class saved.');setModal(null);await load();}catch(err){show(err instanceof ApiError?err.message:'Class could not be saved.','error');}finally{setBusy(false);}}; const saveSection=async(e:FormEvent)=>{e.preventDefault();if(!session||!target)return;setBusy(true);const data={name:sectionForm.name,capacity:sectionForm.capacity?Number(sectionForm.capacity):undefined,status:sectionForm.status};try{if('classId'in target)await sectionApi.update(session.token,target.id,data);else await sectionApi.create(session.token,{...data,classId:target.id});show('Section saved.');setModal(null);await load();}catch(err){show(err instanceof ApiError?err.message:'Section could not be saved.','error');}finally{setBusy(false);}}; const saveBulk=async(e:FormEvent)=>{e.preventDefault();if(!session||!selected)return;setBusy(true);try{await classApi.bulk(session.token,{...bulk,academicYearId:selected.id,sectionCapacity:bulk.sectionCapacity?Number(bulk.sectionCapacity):undefined});show('Academic structure created.');setModal(null);await load();}catch(err){show(err instanceof ApiError?err.message:'Quick setup failed.','error');}finally{setBusy(false);}}; return <><PageHeader title="Classes & sections" description={selected?`Academic structure for ${selected.name}`:'Select or create an academic year first.'} action={<div className="button-row"><PermissionGate code="CLASS_CREATE"><button className="button secondary" disabled={!selected} onClick={()=>setModal('bulk')}>Quick academic setup</button><button className="button" disabled={!selected} onClick={()=>openClass()}>+ Create class</button></PermissionGate></div>} />{loading?<LoadingState/>:!selected?<EmptyState title="No academic year selected" detail="Create an academic year before configuring classes."/>:visible.length===0?<EmptyState title={`No classes in ${selected.name}`} detail="Create a class manually or use Quick Academic Setup."/>:<div className="class-grid">{visible.map(row=><article className="card class-card" key={row.id}><header><div><span className="eyebrow">CLASS</span><h2>{row.name}</h2>{row.code&&<p>{row.code}</p>}</div><StatusBadge status={row.status}/></header><div className="section-heading"><strong>Sections</strong><PermissionGate code="SECTION_CREATE"><button className="text-button" onClick={()=>openSection(row)}>+ Add section</button></PermissionGate></div>{row.sections.length?<div className="section-list">{row.sections.map(section=><button key={section.id} onClick={()=>openSection(row,section)}><span>{section.name}</span><small>{section.capacity?`${section.capacity} seats`:'No capacity set'}</small><StatusBadge status={section.status}/></button>)}</div>:<p className="muted">No sections yet.</p>}<PermissionGate code="CLASS_UPDATE"><button className="text-button edit-class" onClick={()=>openClass(row)}>Edit class details</button></PermissionGate></article>)}</div>}
{modal==='class'&&<Modal title={target?'Edit class':'Create class'} onClose={()=>setModal(null)}><form className="form-grid two" onSubmit={saveClass}><Field label="Class name" required><input value={classForm.name} onChange={e=>setClassForm({...classForm,name:e.target.value})} required/></Field><Field label="Code"><input value={classForm.code} onChange={e=>setClassForm({...classForm,code:e.target.value})}/></Field><Field label="Capacity"><input type="number" min="1" value={classForm.capacity} onChange={e=>setClassForm({...classForm,capacity:e.target.value})}/></Field><Field label="Status"><select value={classForm.status} onChange={e=>setClassForm({...classForm,status:e.target.value})}><option>ACTIVE</option><option>INACTIVE</option></select></Field><div className="span-two"><FormActions submitting={busy} onCancel={()=>setModal(null)}/></div></form></Modal>}
{modal==='section'&&<Modal title={target&&'classId'in target?'Edit section':'Add section'} onClose={()=>setModal(null)}><form className="form-grid two" onSubmit={saveSection}><Field label="Section name" required><input value={sectionForm.name} onChange={e=>setSectionForm({...sectionForm,name:e.target.value})} placeholder="A" required/></Field><Field label="Capacity"><input type="number" min="1" value={sectionForm.capacity} onChange={e=>setSectionForm({...sectionForm,capacity:e.target.value})}/></Field><Field label="Status"><select value={sectionForm.status} onChange={e=>setSectionForm({...sectionForm,status:e.target.value})}><option>ACTIVE</option><option>INACTIVE</option></select></Field><div className="span-two"><FormActions submitting={busy} onCancel={()=>setModal(null)}/></div></form></Modal>}
{modal==='bulk'&&<Modal title="Quick academic setup" onClose={()=>setModal(null)} wide><form className="form-grid" onSubmit={saveBulk}><p className="muted">Select class groups. The backend will create standard classes and the same number of sections for each.</p><div className="choice-grid">{(['montessori','primary','secondary','seniorSecondary'] as const).map(key=><label className={`choice ${bulk[key]?'selected':''}`} key={key}><input type="checkbox" checked={bulk[key]} onChange={e=>setBulk({...bulk,[key]:e.target.checked})}/><strong>{{montessori:'Montessori',primary:'Primary · 1–5',secondary:'Secondary · 6–10',seniorSecondary:'Senior Secondary · 11–12'}[key]}</strong></label>)}</div><div className="form-grid two"><Field label="Sections per class" required><input type="number" min="1" value={bulk.numberOfSections} onChange={e=>setBulk({...bulk,numberOfSections:Number(e.target.value)})}/></Field><Field label="Section capacity"><input type="number" min="1" value={bulk.sectionCapacity} onChange={e=>setBulk({...bulk,sectionCapacity:e.target.value})}/></Field></div><div className="preview"><strong>Preview</strong><p>{Object.entries(bulk).filter(([k,v])=>typeof v==='boolean'&&v).map(([k])=>k).length} class groups selected · {bulk.numberOfSections} section(s) per class</p></div><FormActions submitting={busy} onCancel={()=>setModal(null)} label="Create structure"/></form></Modal>}</>;}
