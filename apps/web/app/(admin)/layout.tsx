'use client';
import type { ReactNode } from 'react'; import { usePathname, useRouter } from 'next/navigation'; import { useEffect } from 'react'; import { AppShell } from '../../components/app-shell'; import { LoadingState } from '../../components/ui'; import { useAuth } from '../../lib/auth-context';
const routePermissions: Record<string, string> = { '/dashboard': 'DASHBOARD_VIEW', '/school/profile': 'SCHOOL_PROFILE_VIEW', '/school/settings': 'SCHOOL_PROFILE_VIEW', '/academics/academic-years': 'ACADEMIC_YEAR_VIEW', '/academics/classes': 'CLASS_VIEW', '/academics/subjects': 'SUBJECT_VIEW', '/access/roles': 'ROLE_VIEW', '/people/staff': 'STAFF_VIEW', '/people/teachers': 'STAFF_VIEW', '/people/parents': 'PARENT_VIEW' };
export default function AdminLayout({ children }: { children: ReactNode }) { const { session, loading, can } = useAuth(); const path = usePathname(); const router = useRouter(); const required = routePermissions[path]; useEffect(() => { if (!loading && session && required && !can(required)) router.replace('/forbidden'); }, [can, loading, required, router, session]); if (loading || !session || session.user.mustChangePassword || (required && !can(required))) return <div className="screen-center"><LoadingState label="Loading your workspace…" /></div>; return <AppShell>{children}</AppShell>; }
