import { Module } from '@nestjs/common'; import { JwtModule } from '@nestjs/jwt'; import { PrismaModule } from '../prisma/prisma.module'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../auth/permissions.guard'; import { TeachersModule } from '../teachers/teachers.module'; import { ClassWorkspaceController } from './class-workspace.controller'; import { ClassWorkspaceService } from './class-workspace.service';
@Module({ imports: [PrismaModule, TeachersModule, JwtModule.register({ secret: process.env.JWT_SECRET || 'davi-super-secret-key', signOptions: { expiresIn: '7d' } })], controllers: [ClassWorkspaceController], providers: [ClassWorkspaceService, JwtAuthGuard, PermissionsGuard] }) export class ClassWorkspaceModule {}
