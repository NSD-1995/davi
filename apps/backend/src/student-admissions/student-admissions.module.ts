import { Module } from '@nestjs/common'; import { JwtModule } from '@nestjs/jwt'; import { PrismaModule } from '../prisma/prisma.module'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { PermissionsGuard } from '../auth/permissions.guard'; import { StudentAdmissionsController } from './student-admissions.controller'; import { StudentAdmissionsService } from './student-admissions.service'; @Module({ imports: [PrismaModule, JwtModule.register({ secret: process.env.JWT_SECRET || 'davi-super-secret-key', signOptions: { expiresIn: '7d' } })], controllers: [StudentAdmissionsController], providers: [StudentAdmissionsService, JwtAuthGuard, PermissionsGuard], exports: [StudentAdmissionsService] }) export class StudentAdmissionsModule {}
