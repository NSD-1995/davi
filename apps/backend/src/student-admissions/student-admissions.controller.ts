import { Body, Controller, Post, Req, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { RequirePermissions } from '../auth/permissions.decorator'; import { PermissionsGuard } from '../auth/permissions.guard'; import { CreateStudentAdmissionDto } from './student-admissions.dto'; import { StudentAdmissionsService } from './student-admissions.service';
@Controller('student-admissions') @UseGuards(JwtAuthGuard, PermissionsGuard) export class StudentAdmissionsController { constructor(private readonly service: StudentAdmissionsService) {} @Post() @RequirePermissions('STUDENT_CREATE') create(@Req() req: any, @Body() dto: CreateStudentAdmissionDto) { return this.service.create(req.user.schoolId, dto); } }
